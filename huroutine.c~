#include <stdio.h>
#include <string.h>
#include <ucontext.h>

int main(int argc, char **argv) {
    struct ucontext_t *u = (struct ucontext_t *)malloc(*u);
    getcontext(u);
    printf("Hello, world!");
    setcontext(u);
    return 0;
}
